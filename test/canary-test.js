var expect = require('chai').expect;
var describe = require('chai').describe;
var it = require('chai').it;

describe('jobumes server tests', () => {
  it('should pass the basic test setup test - Canary test', () => {
    expect(true).to.eql(true);
  });
});
